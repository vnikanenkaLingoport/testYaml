resilience4j:
  circuitbreaker:
    instances:
        availableBooksBreaker: test
        slidingWindowSize: test
        ermittedNumberOfCallsInHalfOpenState: test
        slidingWindowType: TIME_BASED
        minimumNumberOfCalls: test
        waitDurationInOpenState: 50s
        failureRateThreshold: test
        eventConsumerBufferSize: test
      sellsBooksBreaker: circuitbreakerConfig
  retry:
    instances:
      availableBooksRetry: test
        maxAttempts: test
        waitDuration: test
        enableExponentialBackoff: test
        exponentialBackoffMultiplier: test
      sellsBooksRetry: test
